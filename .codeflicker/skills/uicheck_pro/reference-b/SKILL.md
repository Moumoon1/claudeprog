---
name: uicheck-b
description: B端页面走查专用。针对管理后台、数据看板、运营工具等B端页面，重点检查表格、筛选、分页、操作按钮组等结构还原问题。输出正式问题和疑似问题，每条问题在开发图和设计稿中分别框选同一元素。
---

# Goal

1. 找出B端页面中可稳定确认的正式问题（表格列缺失、筛选结构不对、按钮缺失等）
2. 记录疑似问题（行高偏差、间距疑似不同等）
3. 文档输出为表格
4. 双图必须框同一问题对象
5. 图片必须先保存到 workspace

## Reference 文件结构

SKILL.md 放总规则和最高优先级约束，reference-b 按阶段拆分：

| 文件 | 加载阶段 | 内容 |
|------|---------|------|
| `reference-b/issue_rules.md` | analysis | B端问题分类、优先关注、数据排除 |
| `reference-b/false_positives.md` | analysis | B端误判信号、动态数据排除 |
| `reference-b/step1_hint.md` | step1 | B端模块识别细化规则 |
| `reference/output_schema.md` | analysis | JSON 字段规范、输出格式 |
| `reference/runtime_guardrails.md` | analysis | 图片引用格式、防模板污染 |
| `reference/screenshot_rules.md` | 截图 | CropRegion/Box 拆分、元素匹配 |

# Input Identity & Read Verification

- `dev` = 开发实现图，`design` = 设计目标图
- 用户已指定 dev/design 时不得交换；未指定时才可结合文件名与上下文推断
- `@绝对路径` 只负责把本地图片作为附件传入；是否能真正读图取决于当前运行模型是否具备 vision 能力
- 若当前模型是纯文本模型或未开启 vision，必须直接视为"无法读图"，不得因为 prompt 中出现了 `@绝对路径` 就声称已经看到了图片
- 开始判断前，必须先逐一读取两张原图，确认：
  - 图片已成功读取
  - 能区分 dev / design 身份
  - 能看清主要模块与关键元素
- 若任一图片未读到、读错、路径失效、内容不可辨认，必须立即停止并输出：`读图验证失败：[原因]`
- 未完成读图验证前，不得输出正式问题、疑似问题或最终结论
- 分析过程中必须反复保持身份不变：`dev = 已实现结果`，`design = 应还原目标`

# Runtime Guardrails（最高优先级约束）

- 图片输入必须使用 `@绝对路径` 格式；普通文本路径不会自动当作图片读取
- `@绝对路径` 只是附件引用格式，不等于模型一定具备读图能力
- 若当前模型不支持 vision，则应把图片读取视为失败，停止分析，不得宣称模型已读取图片
- 真实 dev/design 图片必须用 `@绝对路径` 放进 prompt，且优先使用独立行块格式
- 分析用中间图片必须优先落到 workspace 内可读目录
- 在调用模型前必须打印并落盘最终 prompt 与最终图片 refs
- 背景信息传纯文本内容，不要把本地文件路径当作背景正文
- 结论只能来自当次截图中可见证据
- 参考文件只提供字段说明、硬约束、排除项和中性模板

> 详细运行时规则见 `reference/runtime_guardrails.md`

# Issue Rules（B端）

B端页面差异分三类：

- 正式问题
- 疑似问题
- 不纳入问题（动态数据 / 滚动 / 状态栏 / 轻微渲染）

优先关注：
- 表格列缺失或列顺序错误
- 筛选栏/搜索框缺失或结构不对
- 分页器位置/样式不一致
- 操作按钮组缺失、顺序或样式不对
- Tab/标签页切换项不一致
- 表头标签名称不一致
- 模块层级/骨架/对齐问题
- 关键状态标签不一致

---

## Design Completeness Rule

必须以设计稿为基准逐模块检查：

- 设计稿中每个模块 / 元素，都必须在开发页确认是否存在
- 若存在于设计稿但开发页不存在 → 必须输出正式问题
- 不得因为未发现差异而跳过模块

---

## Data & Chart Exclusion Rule（B端）

以下差异默认不作为问题：

- 表格中的具体数值（金额、数量、百分比）
- 日期/时间字段的具体内容
- 用户名、头像、ID
- 排名、序号
- 图表数据值（柱高、折线点位、饼图比例）

**但以下属于设计稿固定定义，不属于动态数据，应正常上报：**
- 表头标签名称不同
- 筛选选项文案不同
- 操作按钮文案不同
- Tab 标签名称不同
- 列的增减或顺序变化

对于图表/漏斗：
只检查：结构、标签、单位、图例、样式/布局/层级
不检查：数值一致性、图形形态（漏斗宽度、柱高等）

只有以下情况才算问题：
- 字段缺失
- 标签错误
- 单位错误
- 图例错误
- 结构/样式/层级错误
- 设计稿明确固定形态但未实现

---

# Screenshot Rules

## CropRegion vs Box

坐标输出必须拆分为两组：

- **CropRegion**：裁图区域，略大于问题区域，保留上下文
- **Box**：红框区域，问题元素本身的精确位置，框尽量小而准

规则：
- **devCropRegion 和 designCropRegion 必须完全相同**
- **devBox 和 designBox 必须分别独立定位**，不能复制同一坐标
- 元素级问题 → Box 尽量小，只框具体元素
- 模块级问题 → Box 框住整个出问题的模块
- 不合格的 Box：框整张图（top=0 bottom=1 left=0 right=1）

## Element Matching Rule

双图框选必须基于"元素匹配"，不得基于位置：

1. 先在设计稿识别元素（表格列/按钮/筛选等）
2. 在开发页按语义查找对应元素
3. 找到后再框选

## No Position Projection Rule

禁止：把设计稿框直接映射到开发图同一位置

---

# Problem Description Rule

只允许：
- 问题
- 建议

不允许：
- 标题、位置、长分析
- reference 示例原句
- 截图里不存在的业务词

写法要求：
- 只写当前截图可见差异
- 句子必须可执行、可复核
- 不能因为 reference 出现过某个说法，就直接套到本次问题里

---

# Document Output Rules

结构固定：
1. 标题
2. 摘要
3. 页面总览
4. 问题表格

表头：序号 / 问题描述 / 状态 / 截图 / 日期 / 优先级 / 走查人 / 跟进人

---

# Restrictions

- 不得交换 dev/design
- 不得在未读图验证通过前输出结论
- 不得把动态数据当问题（数值、日期、用户名等）
- 不得把数据驱动图形当问题
- 不得混淆正式 / 疑似
- 不得框错对象
- 不得框空白
- 不得用位置映射代替元素匹配
- 不得把"数据差异"脑补为"规则问题"
- 不得复述 reference 示例中的现成问题句子